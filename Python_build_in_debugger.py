import pdb

x = [12,23,4]
y = 12
z = 2

#put it on top of the code that you think has an error, for example on top of a function or calculations like down below
# it will inspect everything down below not just errors
# press q to exsit the debugger
pdb.set_trace()
a = z + y 
b = x + y
c = y + x
